# Loading data into duckDB

Formatos comunes a lidear cuando se trabaja con data:
- CSV
- JavaScript Object Notation (JSON) 
- Apache Parket Files 

## Topicos del Capitulo 
- Loading CSV files
- Loading JSON files
- Working with Parquet files
- Exploring public datasets
- Exporting data 

## Contenido de files
- (01_loadData.sql)[Chapter_02/01_loadData.sql]: Muestra como crear una tabla e insertar valores de un csv.
- (02_loadDsta.sql)[Chapter_02/02_loadData.sql]: Muestra como adicionar valores a la tabla previa per de un `.tsv` adicionando valores y especificando el tipo de delimitador y que contiene headers el file, aunque eso lo puede asumir duckdb. 
